// Usado no build de produção (ng build)
// Substitua pela URL real do seu backend no Render após o deploy
export const environment = {
  production: true,
  apiUrl: 'COLE_AQUI_A_URL_DO_RENDER/api/prompts'
};
